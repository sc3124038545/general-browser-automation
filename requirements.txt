# ============================================================
# 安装说明
# 国内用户建议使用镜像源加速下载：
#   pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
# 或使用 uv（更快）：
#   uv pip install -r requirements.txt
# ============================================================

# === 核心框架 ===
pydantic>=2.10
fastapi>=0.115
uvicorn>=0.52.4
httpx>=0.28.1
aiofiles>=25.1.0

# === LLM / API 客户端 ===
openai>=1.66
tenacity>=9.1.4
tiktoken>=0.9

# === 配置与日志 ===
tomli>=2.4.1
pyyaml>=6.0.3
loguru>=0.7
structlog
colorama>=0.4

# === 浏览器自动化 ===
playwright>=1.51
browser-use>=0.1.40
pillow>=12.3.0

# === 搜索引擎 ===
googlesearch-python>=1.3
duckduckgo_search>=8.1.1
baidusearch>=1.0

# === 网页处理 ===
requests>=2.34.2
beautifulsoup4>=4.15.0
html2text>=2025.4.15
crawl4ai>=0.9.2

# === 文件与代码 ===
unidiff>=1.0.0

# === MCP 协议 ===
mcp>=2.1.1

# === 沙箱（可选，不需要可注释掉） ===
docker>=7.2.0

# === 云平台（可选，不需要可注释掉） ===
boto3>=1.43.81

# === 数据分析 Agent（可选，不需要可注释掉） ===
numpy>=2.5.2
datasets>=5.0.1
huggingface-hub>=1.29.0

# === Daytona 云沙箱（可选，不需要可注释掉） ===
daytona-sdk

# === 浏览器训练环境（browser-use 内部依赖） ===
gymnasium>=1.3.0
browsergym>=0.13

# === 开发/测试 ===
pytest>=9.1.1
pytest-asyncio>=1.4.0
